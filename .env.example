# Teacher API keys (loaded by `just` via dotenv; never commit .env)
DEEPSEEK_API_KEY=
GLM_API_KEY=
# ZAI_API_KEY=          # fallback for GLM_API_KEY
OPENROUTER_API_KEY=

# Where run data lives (default ~/.local/share/aviary). Never inside the repo.
# AVIARY_DATA_DIR=

# Pinned hermes-agent checkout (lane A only)
# AVIARY_HERMES_DIR=~/projects/hermes-agent
